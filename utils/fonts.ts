import { Poppins, VT323 } from '@next/font/google';

export const poppins = Poppins({
  weight: ['100', '400', '800'],
  display: 'swap',
});

export const vt323 = VT323({ weight: '400', display: 'swap' });
